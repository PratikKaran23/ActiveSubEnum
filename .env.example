# ActiveSubEnum — API Keys
# Copy to .env and fill in. Never commit .env to git.

# ArgosDNS (argosdns.io/dashboard/api-keys)
# WARNING: Limited requests (1000/month). Use --passive-list export instead.
ARGOS_API_KEY=
